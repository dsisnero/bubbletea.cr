require "../spec_helper"

describe "examples/progress_download parity" do
  pending "matches the saved Go golden output exactly (capture harness not implemented yet for progress_download)" do
  end
end
