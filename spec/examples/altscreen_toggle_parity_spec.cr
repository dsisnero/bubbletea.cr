require "../spec_helper"

describe "examples/altscreen_toggle parity" do
  pending "matches the saved Go golden output exactly (capture harness not implemented yet for altscreen_toggle)" do
  end
end
